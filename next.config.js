/** @type {import('next').NextConfig} */
const nextConfig = {
  // Visual rebrand to University of Sydney identity complete
  // No functional changes to Next.js configuration required
};

module.exports = nextConfig;